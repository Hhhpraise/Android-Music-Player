<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Musixx" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ColorText</item>
        <item name="colorPrimaryDark">@color/black</item>

        <item name="android:windowActivityTransitions">true</item>

        <item name="android:windowEnterTransition">@transition/activity_slide</item>
        <item name="android:windowExitTransition">@transition/activity_slide</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="ThemeOverlay.search" parent="Theme.Musixx">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorHint">#FFD3D3D3</item>
    </style>
</resources>